# 總排班表匯入網頁（純前端）開發計劃

## 專案概述

這是一個**駕訓班教練排班**用的網頁專案：使用者**匯入總排班表**（Excel，如全體教練與各案場集中於一張表），網頁讀取後**轉成每位教練個人的月曆行程表**，以日期檢視當日班別、上課地點與期別等資訊。

營運方平時維護一份涵蓋所有教練與案場的總表；教練或行政人員無需從大表中手動篩選自己的列，改以**底部「人員」面板**切換人員（Phase 1 仍為單選下拉，行為同原設計），即可檢視該員橫跨多專案、多地點的排程（純前端、檔案不上傳伺服器）。**人員清單**為各「整欄皆數字」欄第 1 列彙整之**首列人名清單**，並於清單中**固定追加一項「其他」**；解析各資料列排班時，專案區塊指派格文字若落在清單內或經 **§4.5 智慧對應** 可對至清單內人員則建立該員事件，否則建立至**「其他」**之彙總檢視（該月曆格子須顯示儲存格上讀到之**人名**文字；細節見第 2、4、5 節）。版面改版細節見 `[ui_develop_plan.md](./ui_develop_plan.md)`。

---

## 1. 目標與範圍

- **目標**：使用者於瀏覽器選擇 `shiftTotalTable.xlsx`（或同版面之 xlsx），網頁解析後以**人**為單位顯示個人班表（含**「其他」**彙總清單外人名指派）；header 提供「匯入總排班表」（進站首要操作，常駐頂部）及**目前人員摘要**（如 `人員：維鈞`，隨 header 捲動、不 sticky；未來多選時如 `人員：維鈞、鴨子`）；**底部「人員」面板**切換人員（預設第一人）；**底部「檢視」面板**提供「只顯示本月及未來月份」、「顯示無資料月份」等選項，預設僅顯示**當月及之後**有排程之月份，取消勾選後改為顯示**全部**月份；相鄰有資料月份之間若曆月連續但無排程，預設以**幽靈月份**區塊標示（見第 5 節），避免使用者誤以為漏月；每日格子內可有多張「班別卡」。底欄另有「更多」預留匯出等功能（Phase 1 點擊暫無反應）。
- **範圍**：**僅前端**（靜態 HTML/JS 或極輕量本地開發伺服器皆可），**無後端**、不上傳檔案至任何伺服器。
- **視覺與互動**：月曆區塊與卡片樣式對齊現有 `example/calendar.html`（週一為首、月區塊、圖例可視需求沿用或簡化）。

---

## 2. 輸入檔假設（依實檔與規則整理）


| 項目         | 說明                                                                                                                                                                                                                                                                                                             |
| ---------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 工作表        | **固定讀取第一張工作表**（不論工作表名稱為何）。                                                                                                                                                                                                                                                                                     |
| 表頭列        | **第 1 列**：專案名稱；**第 2 列**：該專案對應之**上課地點**（與欄位對齊）。                                                                                                                                                                                                                                                                |
| 「期別」欄      | 若第 1 列為 **「期別」**（與業務約定完全一致字串），該欄自資料列起為**左側鄰接專案區**之期別；**不作為人名**處理。**版面約定**：期別欄一律位於其所屬專案欄的**左側**（同一專案區島內、欄索引較小）；解析時取「小於該專案欄索引」中，**最靠右**（與專案欄距離最近）的那一欄「期別」為該專案之期別來源。**相鄰專案無期別**：若由左而右先出現專案 A 欄、再出現專案 B 欄，且 A、B 兩欄之間（不含 A、B 兩欄本身）**沒有任何**第 1 列為「期別」的欄，則視為 **B 沒有期別資料**（該專案事件之 `period` 為空），即使更左側仍有其他「期別」欄亦不可挪用到 B。 |
| 「整欄皆數字」欄   | 若某欄第 1 列**不是**「期別」，且該欄自資料列起**皆為數字**、**空＋數字**、或**空白**（「數字」定義見風險章節），則第 1 列該格視為**人名**（該欄為「以人為欄」之版面辨識特例）。**此欄自資料列起之儲存格不視為排班內容**，解析班表時可忽略。                                                                                                                                                                          |
| 首列人名清單與排班格 | **首列人名清單**：僅由各「整欄皆數字」欄第 1 列之人名彙整而成；**下拉選單**在清單尾端固定追加一項**「其他」**（不來自表頭，為系統虛擬項）。解析各資料列之排班內容時：指派格文字若與清單**完全一致**或經 §4.5 智慧對應 可對至清單內人員，建立至該員（複合可對多人）；若**無法對應**或為排班備註／活動名，建立至**「其他」**（仍視為有效指派、**不**將該字串併入首列人名清單）。                                                                                                     |
| 資料列        | 自第 **3** 列起為每日資料（與先前分析一致：左側有 **Excel 日期序號** 表示該列日期；若實檔有雙日期欄，取主要一欄或兩欄一致時才採用）。                                                                                                                                                                                                                                   |
| 同日同人多格     | 若同一天、同一人在**不同欄**（不同案場／專案）各有一格有效內容，則在該日日曆格子內渲染 **多張卡**（垂直堆疊），每張卡對應一筆 `(專案, 地點, 期別若有)`。                                                                                                                                                                                                                          |


---

## 3. 技術選型（純前端）


| 項目      | 建議                                                                                   |
| ------- | ------------------------------------------------------------------------------------ |
| 解析 xlsx | **SheetJS**（`xlsx`）於瀏覽器以 `ArrayBuffer` 讀檔；或 **ExcelJS**（較重但合併儲存格較友善）。                |
| 執行環境    | 單一 `index.html` + `app.js` + `styles.css`，或以 Vite 等建置工具（仍僅靜態輸出、無 API）。               |
| 日期      | Excel 序號轉 JS `Date` 時注意 **1900/1904 日期系統**；對外鍵一律 `YYYY-MM-DD`（與 `calendar.html` 一致）。 |
| 部署      | GitHub Pages、任意靜態主機、或本機 static server（避免僅 `file://` 的限制）。                            |


---

## 4. 核心演算法（解析 → 資料模型）

### 4.1 讀取與二維矩陣

1. `input[type=file]` 取得檔案 → `arrayBuffer()`。
2. 讀活頁簿 → **取第一個工作表** → 轉成二維陣列 `grid[row][col]`（列 0、1 為表頭）。
3. （建議）讀取**合併儲存格**，用於第 1～2 列表頭之「向右／向下填滿」；否則以由左而右之「專案區塊」推導每欄所屬專案／地點。

### 4.2 欄位語意：專案、地點、期別、人名欄

對每個欄索引 `c` 建立欄位角色與表頭語意。
**規則實作順序建議**：

1. **期別欄**：若第 1 列為 **「期別」**（與業務約定完全一致字串），標記 `columnRole[c] = 'period'`。與專案之對應：**僅**使用該專案欄左側、同島內且欄索引較小之**最靠右**「期別」欄之儲存格值。若左側已出現另一專案主欄，且兩專案欄之間沒有任何「期別」欄，則右側該專案**不帶期別**（見輸入檔假設表「期別」列）。
2. **專案主欄**：第 1 列有非空文字且非上述期別欄者，預設為專案區塊之欄；`projectName[c]` 取第 1 列，`locationName[c]` 取第 2 列（可為空；空則月曆不顯示地點該行，見「月曆格子內文顯示」）。
3. **「整欄皆數字 → 第 1 列是人名」**：在鎖定 `dataStartRow`（例如第 3 列，0-based 為 2）之後，掃描該欄所有資料列：若皆為「數字、空＋數字、或純空白」且符合輸入檔假設之辨識條件，則 `columnRole[c] = 'personColumn'`，並將第 1 列之文字**加入首列人名清單**；**該欄資料列不產生班表事件**（與假設一致）。**與「期別」欄衝突時，以「期別」優先**。
4. **期別與專案**：資料列上，期別僅來自**專案欄左側**之「期別」欄（同島、小於專案欄索引之最右一欄）；**不**採用專案欄右側之期別，**不**採用「距離最近」等雙向啟發式。若與本專案欄之間僅隔著另一專案欄而無「期別」欄，則本專案當日事件之 `period` 為空。

> **跨欄專案**：實檔常見「專案」跨多欄；以 **合併儲存格** 或 **「專案欄為左界、下一專案欄為右界」** 切出橫向 **block**，於 block 內再區分期別子欄與指派格子。

### 4.3 資料列掃描（逐日）

對每個資料列 `r`：

1. 由最左欄讀取該列 **日期**（Excel 序號或日期型別）→ `dateStr`；無效則 skip。
2. 依 block／欄位角色掃描：
  - **人名欄**（整欄數字規則）：**不**依該欄資料列產生事件；僅用於辨識第 1 列人名並納入**首列人名清單**（見輸入檔假設）。
  - **專案區塊內之指派格**：儲存格文字先經 **§4.5 切分與人名對應** 處理。若可對應至首列人名清單內之一人或多人，建立至該員（複合字串見 §4.5.4）；若**無法對應任何人**或判定為**排班備註／活動名**（非人名），則建立一筆歸屬於**「其他」**之事件，並保留該片段之**原始文字**作為 `rawName`；**不**將該字串加入首列人名清單。
3. **同人同日多筆**：`byPerson[name][dateStr] = [ { project, location, period?, rawName? }, ... ]`（`name` 為清單內人名或固定字串**「其他」**；歸「其他」時 `rawName` 必填；經智慧對應歸人時**建議仍帶 `rawName`** 以保留儲存格原文），每於專案區塊指派格命中一格 **push** 一筆 → UI **多張卡**。

### 4.4 邊界

- 表尾無日期之列：終止掃描。

### 4.5 清單外人名智慧對應（規格，已實作）

實務上指派格常出現**簡寫**（如「維」表「維鈞」）、**別名**（如「歹徒」表「廖/歹徒」）、**備註後綴**（如「維鈞半天」）、**多人連寫**（如「威駿瑩」），以及**日期班別備註**（如 `3/23只有下午`）。本節定案下列行為；**已實作於** `src/resolvePersonNames.js`，由 `parseShiftWorkbook` 呼叫。

#### 4.5.1 處理流程（pipeline）

對每個指派格讀出之 `rawCell`：

1. `**splitAssignmentTokens`**：依分隔符切為多個 token（規則見 §4.5.2）。
2. 對每個 **token**：
  - 若為 **排班日期備註**（§4.5.2）→ 整段歸 **「其他」**，`rawName` 為完整原文，**不**嘗試對人。
  - 否則 **正規化**（剝活動前綴、班別後綴、夾雜數字等，§4.5.3）→ `**resolvePersonNames(token)`**（§4.5.3～4.5.4）。
  - **0 人** → `pushEvent('其他', …, { rawName: token })`（`token` 為切分後片段原文）。
  - **1 人** → `pushEvent(person, …, { rawName: token })`。
  - **N 人（複合）** → 見 §4.5.4。

#### 4.5.2 切分 token 與日期備註

**現況**：以 `[,./、／]` 等切分，半形 `/` 會把 `3/23只有下午` 切成 `3` 與 `23只有下午`，兩段皆誤進「其他」。

**定案**：

1. **整段日期備註**（切分前或切分後若某 token 單獨符合）視為非人名，**不切、不對人**，整段歸「其他」並保留完整 `rawName`：
  - 建議正則：`^\d{1,2}/\d{1,2}(只有)?(上午|下午|半天)?$`（例：`3/23只有下午`）。
2. **保護「數字/數字」中的斜線**：切分前將 `(\d{1,2})/(\d{1,2})` 暫替為佔位字元，切完再還原；或切分時**不**把「兩側皆為數字」的 `/` 當人名分隔符。
3. 其餘仍可用 `,`、`.`、`、`、全形 `／` 等切分；半形 `/` 僅在**非**「月/日」型態時可作分隔（若實務出現 `俊/駿` 這類寫法）。

**與數字夾人名之區別**：`孫12遠1` 等應在**正規化**階段移除班表用數字後再做人名切分，**不是** `/` 切分問題。

#### 4.5.3 人員索引與正規化

**人員索引**（自表頭 `people` 自動建立，**不做**手動別名檔／localStorage 學習）：


| 來源    | 說明                                                     |
| ----- | ------------------------------------------------------ |
| 正規名   | 去掉尾端 `.`（`駿.` → `駿`）                                   |
| 斜線別名  | `廖/歹徒` → `廖`、`歹徒`                                      |
| 慣用單字  | 如 `鴨子→鴨`、`呂小遠→遠`、`林欣瑩→瑩/欣`、`林威→威`、`字鈞→字`、`維鈞→維`、`俊穎→俊` |
| 首字／末字 | 二字以上人名可登錄首字、末字（處理「俊」→ 俊穎等）                             |


**歧義**：同一別名對到多人時**不硬配**，該 token 歸「其他」（或僅在分數明顯領先時採唯一前綴匹配，實作時需寫清規則）。

**正規化**（對人名 token，在 `resolve` 前）：

- 剝 **活動前綴**：`考試一`、`警專三考`、`考` 等（例：`考鴨` → `鴨`）。
- 剝 **班別後綴**：`半天`、`上午`、`下午`、`保養` 等（例：`維鈞半天` → `維鈞`）；必要時剝 `**^半天`** 前綴（例：`半天駿保養` → `駿`）。
- 移除 **夾雜數字**與 `**上`/`下`**（半天標記，例：`駿上俊穎下`）。
- **整段黑名單**（非人名）：`大台北`、`期中考`、`期末考`、`特考班…測驗/考周` 等 → 歸「其他」，不擴充清單。

**解析順序**：別名精確命中 → 唯一前綴／整段匹配 → **最長別名優先**之貪婪切分（複合字串，§4.5.4）。切分殘段無法解釋時，已識別者歸人、殘段歸「其他」。

#### 4.5.4 複合字串（對到多人）

例：`威駿瑩` → 林威、駿、林欣瑩；`俊遠孫鴨` → 俊穎、呂小遠、孫、鴨子。

**定案**：

- **同一格班別事件複製多筆**：對每位識別到的人各 `pushEvent` 一筆，`project` / `location` / `period` 相同。
- **每筆皆帶同一 `rawName`**（該 token 原文，供月曆顯示當初寫法）。
- **先不做** UI「與他人共班」標籤。

與既有去重：同日同人同專案／地點／期別（含 `rawName`）仍只保留一筆（見 `parseShiftWorkbook` 之 `eventDedupKey`）。

#### 4.5.5 仍歸「其他」之典型情況

- 表頭人員欄**不存在**且無法由規則推斷者（例：實檔常見之 `拳頭`、`雷`；需營運確認前維持「其他」）。
- 活動名、考試週、地點備註等（§4.5.3 黑名單）。
- 別名歧義且無法安全消解者。

**驗收參考**：`example/shiftTotalTable0515_01.xlsx` 中，簡寫／別名／複合／備註後綴應盡量歸人；`3/23只有下午` 須維持**單一**「其他」事件且 `rawName` 為完整字串。實作後以該檔回歸並可另附對照表。

#### 4.5.6 正規人員鍵（表頭尾端「.」合併）

實檔表頭可能同時出現 `**維鈞`** 與 `**維鈞.`**（以及 `駿`／`駿.`、`俊穎`／`俊穎.` 等）。**解析與 UI 均以「正規人員鍵」為準**，避免同一人被拆成兩個選項、兩份月曆。


| 項目            | 規則                                                                                            |
| ------------- | --------------------------------------------------------------------------------------------- |
| **正規人員鍵**     | 表頭人名去掉尾端 `.` 後的字串（`維鈞.` → `維鈞`）；作為 `byPerson` 的 key、人員 dialog 的 `value`、header `人員：{名稱}` 的顯示名 |
| **表頭掃描**      | 讀到 `維鈞.` 與 `維鈞` 時，**只保留一筆**正規鍵 `維鈞` 於內部人員清單（順序可取表頭**首次出現**之欄位順序）                              |
| **指派格完全一致**   | 儲存格為 `維鈞.` 且表頭亦有 `維鈞.` 時：事件歸 `**維鈞`**；若原文與正規鍵不同，帶 `rawName: '維鈞.'`（見「月曆格子內文顯示」）               |
| **§4.5 別名對應** | 索引與 `resolvePersonNames` 皆以正規鍵為歸屬目標；`維鈞.`、`維` 等 token 對到之人員皆寫入 `byPerson['維鈞']`               |


> **人員選擇 dialog**（§5）僅列出正規鍵，**不**出現 `維鈞.` 與 `維鈞` 兩個 radio；選「維鈞」時月曆含兩種表頭欄位來源與指派格簡寫／別名對應之所有事件。

#### 4.5.7 實作範圍外（本階段不做）

- **手動別名表**、使用者確認後寫入 localStorage 等「學習」機制。
- 月曆卡上「與他人共班」標示。

---

## 5. UI 開發項目

> **版面導航**（見 `[ui_develop_plan.md](./ui_develop_plan.md)`）：底部固定 **人員｜檢視｜比較｜更多** 四鍵（「比較」規格見 **第 10 節**）；人員／檢視／比較以置中 Dialog 承載；**匯入**保留在 **header**。Phase 1「更多」按鈕可見但點擊暫無反應。


| 項目       | 位置                                                  | 說明                                                                                                                                                                                               |
| -------- | --------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| 匯入       | **header**                                          | 按鈕「匯入總排班表」觸發隱藏 `input file`，`accept` 限 xlsx（或含 xls，視函式庫）。進站後首要操作，**不遷移**至底欄。                                                                                                                     |
| 錯誤提示     | **header**                                          | 無法解析、第一張表無資料時顯示清楚訊息。                                                                                                                                                                             |
| 目前人員顯示   | **header** `#person-summary`                        | 唯讀文字 `**人員：{名稱}`**；與底欄人員 sheet 選擇同步。**Phase 1** 單選：例 `人員：維鈞`、`人員：其他`。**未來多選**：以全形頓號串接，例 `人員：維鈞、鴨子`。未匯入時隱藏。隨 header 捲動，**不 sticky**。                                                              |
| 人員選擇     | **底欄 → 人員 dialog**（radio 清單，見 `ui_develop_plan.md`） | 解析後依**正規人員鍵**清單填入（§4.5.6）：表頭若同時有 `維鈞` 與 `維鈞.`，dialog **只顯示一項「維鈞」**（以此類推 `駿`／`駿.`、`俊穎`／`俊穎.` 等）；清單**尾端固定追加「其他」**；**預設第一筆**。選取「其他」時，月曆僅顯示清單外指派彙總。變更後更新 header 人員摘要。未來改為多選（見 `ui_develop_plan.md`）。 |
| 月份篩選     | **底欄 → 檢視 sheet**                                   | 核取方塊文案：**「只顯示本月及未來月份」**；**預設勾選**。**勾選時**：僅顯示「本月」（以瀏覽器**本日**所屬曆月為準）及**之後**、且當前人員有排程事件之月份。**未勾選時**：顯示該人員**所有**含事件之月份（含本月之前）。變更後即時重繪月曆。                                                             |
| 無資料月份    | **底欄 → 檢視 sheet**                                   | 核取方塊文案：**「顯示無資料月份」**；**預設勾選**。**勾選時**：於畫面上**相鄰兩個有資料月**之間，對曆月連續但無事件之月份各插入一**幽靈月份**區塊（見下節）。**未勾選時**：僅渲染有排程之完整月曆（現行行為）。與月份篩選連動，見下節規則 3。                                                             |
| 與上一版比較   | **底欄 → 比較 dialog**                                  | 見 **第 10 節**（待實作）：匯入上一版、全體摘要／變更卡；關閉後月曆 diff（方案 2＋1b）；主內容區目前人員摘要／變更卡。                                                                                                                             |
| 更多（預留）   | **底欄 → 更多**                                         | Phase 1：**點擊無反應**。未來：匯出 PDF、Excel／CSV 等（**不含**匯入）。                                                                                                                                               |
| 月曆       | 主內容區                                                | 依當前人員收集日期 → 套用檢視 sheet 之月份篩選 → 組裝「顯示用月份序列」（含**有資料月**與可選之**無資料月**）→ 有資料月呼叫 `buildMonth`、無資料月呼叫 `buildEmptyMonthPlaceholder`；單日 **forEach** 渲染多個 `event-card`。                                     |
| 樣式       | 全站                                                  | 自 `example/calendar.html` 抽出 CSS；專案字串 map 至 `proj-`* class；含底欄與 sheet。                                                                                                                           |
| 圖例       | 主內容區（header 下）                                      | 動態依專案生成；比較模式另加 diff 圖例（第 10 節）。                                                                                                                                                                  |
| 顯示與上一版差異 | **底欄 → 檢視 dialog**                                  | Checkbox，**預設勾選**；見第 10 節。未匯入上一版時於 label 文案**後方**灰字提示；未勾選時月曆僅新版。                                                                                                                                                             |
| 目前人員比較摘要 | **#legend 與第一個月曆之間**                                | 僅在已匯入上一版且勾選「顯示與上一版差異」時；該員摘要卡（**不含**「M 位人員」）＋變更卡（第 10 節）。                                                                                                                                           |


### 無資料月份（幽靈月份）

相鄰兩個**有排程事件**的曆月之間，若中間存在一個或多個**完全無事件**的曆月，預設仍要在時間軸上標出這些月份，避免使用者誤以為系統漏載或跳月（例如有 6 月與 9 月資料時，中間應出現 7 月、8 月各一區塊）。

**呈現方式（方案 B）**

- **每個無資料月份各一區塊**（不將多個空月合併為單一提示卡），讓使用者不必細讀文字也能從月份標題連續性判斷時間。
- 區塊**僅含月份標題列與一行說明**，**不**渲染 `cal-grid`、**不**畫日期格（避免與「整月無資料」混淆為「逐日空白」）。
- 建議 DOM 結構：外層 `.month-section.month-section--empty`；標題列 `.month-header.month-header--empty` 顯示 `YYYY年 M月`；其下 `.month-empty-body` 顯示 `**無排班資料`**（字級略小、灰色）。

**視覺**

- 與完整月曆 `.month-section` 同寬（`max-width: 900px`）、同圓角與卡片陰影，維持列表節奏一致。
- 標題列改為**淺色／灰階**（**非**有資料月之深藍 `#1a1a2e` 標題），與完整月曆明確區分。
- 整體高度遠小於含 `cal-grid` 的完整月曆。

**插入規則**

1. **僅填「相鄰有資料月份之間」的空隙**：自當前人員、經「只顯示本月及未來月份」篩選後之月份清單，依時間排序為 `M₀, M₁, …, Mₙ`。對每一對相鄰的 `(Mᵢ, Mᵢ₊₁)`，若兩者曆月差大於 1，則對中間每一曆月各插入一筆幽靈月份（逐月遞增，含跨年，例：2025 年 12 月與 2026 年 2 月之間插入 2026 年 1 月）。
2. **不**在清單**最前**（第一個有資料月之前）或**最後**（最後一個有資料月之後）插入幽靈月。
3. **與月份篩選連動**：幽靈月只出現在**畫面上實際相鄰的兩個有資料月**之間。若篩選導致某有資料月被隱藏，則不為「已隱藏月」與其外側月份之間補幽靈月。
  **例**：有 6 月與 9 月資料且「顯示無資料月份」已勾選；若「只顯示本月及未來月份」使 6 月不顯示、畫面僅剩 9 月，則**不**在 9 月上方單獨顯示 7、8 月；若 6 月與 9 月**皆**在畫面中，則兩者之間依序顯示 7 月、8 月幽靈區塊。
4. **與「顯示無資料月份」開關連動**：未勾選時不插入幽靈月，行為與現況相同（僅 `buildMonth` 有事件之月份）。

**實作備註（供開發參考，確認規格後再改程式）**

- `collectMonths` 仍只收集**有事件**之月份；另新增 `expandMonthsWithGaps(months)`（或同等函式）產生 `{ type: 'data' | 'empty', year, month }[]`。
- `renderCalendar` 依 `type` 分支：`data` → `buildMonth`；`empty` → `buildEmptyMonthPlaceholder`。
- 檢視 sheet 新增 `#show-empty-months`（文案：**顯示無資料月份**），預設 `checked`；`change` 時重繪月曆。狀態可對應 `ui_develop_plan.md` 之 `showEmptyMonths`（預設 `true`）。

### 月曆格子內文顯示

每日格子內之**每一張班別卡**（每一筆排班事件）欄位順序與空行規則如下。

#### 選取清單內人員（含 §4.5 別名對應後歸入該員者）

- **預設**（無 `rawName`，或 `rawName` 與目前選取之正規人員鍵相同）：由上而下 **專案名稱 → 地點 → 期別**（與現行一致）。
- **當事件含 `rawName` 且與目前選取人員之正規鍵不完全相同**（例如選「維鈞」，該筆來自儲存格 `維鈞.`、`維`、`維鈞半天` 等）：卡片**最上方**先顯示 `**rawName` 原文**（樣式同 `.raw-name-line`），其下再依序 **專案名稱 → 地點 → 期別**。
**例**（選人員「維鈞」、該筆 `rawName` 為 `維鈞.`）：
  ```
  維鈞.
  [專案名稱]
  [地點]
  [期別]
  ```
- **地點**與**期別**在資料上可能為空；若該項無有效內容，則**不顯示該行**，亦**不留空白行**。
- 專案名稱依解析結果顯示；若同日多筆，每一張卡各自依上列規則組字。

#### 選取「其他」

- 每一張班別卡須顯示該筆指派格讀到之**人名**（`rawName`，即無法對應或刻意歸「其他」之原文），並可搭配專案名稱、地點、期別；**人名置於最上**（人名 → 專案名稱 → 地點 → 期別），空行規則同上。

#### 實作備註

- `fillEventCard`：將「僅在 `personKey === '其他'` 時顯示 `rawName`」改為：**「其他」必顯示；清單內人員則依上列條件顯示**。
- 比對「是否與選取人相同」時，以**正規人員鍵**（去掉尾端 `.`）為準，避免 `維鈞` 與 `維鈞.` 重複顯示兩行卻內容重複。

---

## 6. 測試計劃（建議）


| 案例                                    | 預期                                                                                                                            |
| ------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------- |
| `example/shiftTotalTable.xlsx`        | 多人可選；抽查姓名與日期。                                                                                                                 |
| 同人同日兩格不同專案                            | 兩張卡。                                                                                                                          |
| 僅期別無人名之列                              | 不崩潰、不誤加人名。                                                                                                                    |
| 選取「其他」                                | 僅見清單外指派；每張卡顯示該格**人名**文字及專案／地點／期別。                                                                                             |
| 清單外人名智慧對應（§4.5）                       | 簡寫（`維`→維鈞）、別名（`歹徒`→廖/歹徒）、備註後綴（`維鈞半天`）、複合（`威駿瑩`→多人各一筆且帶相同 `rawName`）；`3/23只有下午` **不得**切成 `3` 與 `23只有下午`，整段在「其他」且 `rawName` 完整。 |
| 正規人員鍵與人員 dialog（§4.5.6）               | 表頭同時有 `維鈞`、`維鈞.` 時，dialog 僅一項「維鈞」；選維鈞可見兩欄來源之班；指派格 `維鈞.` 歸維鈞且卡片最上顯示 `維鈞.`。                                                     |
| 清單內人員卡片 `rawName`（月曆內文）               | 選維鈞、事件 `rawName` 為 `維鈞半天` 時，卡片最上為 `維鈞半天`，其下專案／地點／期別；`rawName` 為 `維鈞` 時不重複顯示最上行。                                               |
| `example/shiftTotalTable0515_01.xlsx` | 作為 §4.5 回歸樣本；可對照「其他」rawName 與預期歸屬人員。                                                                                          |
| 「首列人名、整欄數字」欄                          | 正確分類為人名欄、不誤判為專案名；**該欄資料列不產生班表卡**；專案格內**清單外**文字產生至「其他」視圖之卡、顯示該人名、不擴充首列人名清單。                                                    |
| 「只顯示本月及未來月份」                          | 預設勾選：過去月份之月曆區塊不出現；取消勾選：過去與未來凡有資料之月皆出現；切換後畫面與月份順序正確。                                                                           |
| 無資料月份（幽靈月份）                           | 有 6 月與 9 月資料、「顯示無資料月份」勾選：兩完整月曆之間依序出現 7 月、8 月幽靈區塊（僅標題＋「無排班資料」、無日期格）；取消「顯示無資料月份」則 6 月直下 9 月。                                    |
| 無資料月份 × 月份篩選                          | 6 月被篩掉、僅 9 月在畫面：不出現 7、8 月幽靈區塊；6 月與 9 月皆顯示時：中間出現 7、8 月。跨年：2025/12 與 2026/2 之間出現 2026/1。                                        |
| header 目前人員摘要                         | 匯入並選人後顯示 `人員：{名稱}`；切換人員後文字同步；捲動頁面時與 header 一併捲走（非固定頂端）。                                                                       |


---

## 7. 交付物清單

- 前端專案目錄（如 `web/`）。
- `parseShiftWorkbook(arrayBuffer)` → `{ people, byPerson }`（`people` 為**去重後之正規人員鍵**清單，§4.5.6；UI 人員 dialog 同清單並含尾端**「其他」**。`byPerson` 的 key 一律為正規鍵。`byPerson['其他']` 收錄無法對應人員之指派；經 §4.5 對應者寫入各員 `byPerson` 並可帶 `rawName`。建議將 `resolvePersonNames`／索引建立獨立模組以便單元測試。）
- 主頁：header 匯入 + 目前人員摘要（`#person-summary`）；底欄人員／檢視 sheet（含人員選擇、月份篩選、**顯示無資料月份**）、月曆；底欄「更多」預留（Phase 1 無功能）。
- `buildEmptyMonthPlaceholder(year, month)` 與 `expandMonthsWithGaps`（或同等邏輯）：幽靈月份 UI。
- `[ui_develop_plan.md](./ui_develop_plan.md)`：UI 改版階段與未來功能擺放位置。
- 樣式對齊 `example/calendar.html`。
- （選配）README：本機預覽方式。

---

## 8. 風險與待確認事項

1. **「整欄都是數字」**是否含日期序號、公式結果、小數 → 包含日期序號、公式結果、小數
2. **人名欄**資料格若偶爾出現非數字，是否改為註解或忽略 → 是
3. **合併儲存格**未處理時，專案／地點可能只出現在區塊第一欄 → **必做填滿或讀 merge**。
4. **人名欄與專案欄分工**：依輸入檔假設，人名欄資料列不產生事件；專案／地點僅來自專案區塊指派格；指派格經 §4.5 可對應者掛該員，**否則掛「其他」並保留儲存格人名供顯示**。
5. **§4.5 簡寫歧義**：單字別名若多人符合且無安全消解規則，寧可留「其他」勿誤配。
6. 表頭「民國年」與儲存格西元日期可能不一致 → 以儲存格解析出的西元 YYYY-MM-DD 為準（含該年、該月、該日與由此推算的星期）。不要依檔名、工作表名稱或表頭民國年文字推斷日期。

---

## 9. 建議開發順序

1. SheetJS 讀檔 → 第一張表 → 除錯印出前 30 列。
2. 欄位角色 + block 切分（含期別、人名欄與首列人名清單）。
3. 列 → 日期 + `byPerson[name][date][]`。

3a. ~~**§4.5 清單外人名智慧對應**~~（**已完成**：`src/resolvePersonNames.js`；`splitAssignmentTokens` 日期保護、`resolvePersonNames`、複合複製多筆、**§4.5.6 正規人員鍵**；以 `shiftTotalTable0515_01.xlsx` 回歸）。
3b. **月曆卡片 `rawName`**：清單內人員在原文與選取人不同時最上顯示 `rawName`（見「月曆格子內文顯示」）。
4. 人員 dialog（正規鍵清單）+ 月份篩選核取方塊 + `buildMonth` + 多卡。
5. **無資料月份**：`expandMonthsWithGaps` + `buildEmptyMonthPlaceholder` + 檢視 sheet「顯示無資料月份」開關（預設開）；樣式（`.month-section--empty` 等）。
6. 以 `example/shiftTotalTable.xlsx` 回歸測試。
7. 收斂錯誤訊息、移除除錯。
8. （UI 改版）底欄 + 人員／檢視 sheet；header 匯入 + `#person-summary`；詳見 `ui_develop_plan.md` Phase 1。
9. **與上一版比較**（見第 10 節）：diff 模組、`#dialog-compare`、月曆方案 2＋1b、主內容區目前人員摘要／變更卡；確認規格後再實作。

---

## 10. 與上一版總排班表比較（規格，待實作）

> **狀態**：產品與 UI 已定案；**尚未開發**。實作前以本節為準；版面導航細節可與 `[ui_develop_plan.md](./ui_develop_plan.md)` 交叉對照（底欄由三鍵擴為四鍵）。

### 10.1 目標與資料角色

使用者於頂部匯入**新版**總排班表後，可另匯入**上一版** xlsx，在瀏覽器內比對兩版**解析後排班語意**之差異（非 Excel 儲存格逐格 diff），以便快速掌握「哪幾天、哪幾人、班別有何增刪改」。


| 版本      | 來源                    | 變數（建議）               | 用途                                             |
| ------- | --------------------- | -------------------- | ---------------------------------------------- |
| **新版**  | Header「匯入總排班表」        | 現有 `parsed`          | 月曆主內容；比較時為對照中的**新版**（變更卡**右欄**）                |
| **上一版** | 「比較」dialog「匯入上一版總排班表」 | `baselineParsed`（建議） | 僅供 diff；**不**取代新版月曆；比較時為對照中的**上一版**（變更卡**左欄**） |


兩版皆呼叫既有 `parseShiftWorkbook(arrayBuffer)`，資料結構同 `{ people, byPerson }`（事件欄位：`project`、`location`、`period`、`rawName?`）。

**比對人員範圍（已定案）**：`compareSchedules` 以兩版 `byPerson` 的 **person key 聯集** 為準（含固定字串**「其他」**；「其他」桶內事件仍以 `rawName` 區分比對 key，但人員 key 仍為 `其他`）。**不**限於新版 `parsed.people`。

**僅存在於上一版的人員（已定案）**：若某人僅在上一版 `byPerson` 有資料、不在新版表頭／`parsed.people` 內，其變更**僅**出現在比較 dialog 之**全體**變更卡與摘要；底欄人員 dialog **不提供**該員選項，月曆與 `#person-diff-panel` **無法**切換檢視（產品接受）。

### 10.2 導覽與入口


| 項目  | 規格                                                                                                                                                              |
| --- | --------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 位置  | 底欄／右側導覽列，順序：**人員｜檢視｜比較｜更多**（「比較」在「檢視」與「更多」之間）                                                                                                                   |
| 互動  | 與人員／檢視相同：點擊開啟**置中 Dialog**（`#dialog-compare`）；遮罩、Esc、關閉鈕關閉；`data-dialog="compare"`                                                                              |
| 清除上一版 | Phase 1 **不提供**「清除上一版」按鈕；僅以 **重新選擇** 覆寫，或重新整理頁面重置（見 §10.9）                                                                                              |
| DOM | `#dialog-overlay` 內新增 `#dialog-compare`；建議 modifier `.dialog--compare`（較高、略寬如 `min(92vw, 560px)` 以容納左右對照變更卡；`max-height: min(90vh, 720px)`，`.dialog__body` 可捲動） |


### 10.3 「比較」Dialog 內容與狀態

**標題**：`與上一版總排班表比較`

#### 狀態 A：尚未匯入上一版

- 主按鈕：**匯入上一版總排班表**（觸發隱藏 `input[type=file]`，`accept` 同主匯入）。
- 說明小字：檔案僅在瀏覽器解析；請先以頂部按鈕匯入**新版**。
- 若 `parsed === null`：**禁用**匯入上一版按鈕，並提示先匯入新版。

#### 狀態 B：解析中

- 按鈕文案「解析中…」、`disabled`；可選簡短 loading。

#### 狀態 C：解析完成

1. **檔案列**：`上一版：{檔名}` ＋ 文字連結 **重新選擇**（不關 dialog）。
2. **摘要卡**（**全體**、跨所有人員）
  - 例：`共 N 處變更`；`M 位人員 · K 個日期`；`新增 a · 刪除 b · 修改 c`（`N === 0` 時仍顯示，數字皆為 0，例：`共 0 處變更`；`0 位人員 · 0 個日期`；`新增 0 · 刪除 0 · 修改 0`）。
3. **變更卡列表**（**全體**；在 dialog 內捲動；版面見 **§10.3.1**）
  - 若 `N === 0`：**不**列變更卡；列表區僅一行 **「兩版相同，無變更」**（仍保留上方檔案列與摘要卡）。
  - 若 `N > 0`：每張卡標題列：日期（含星期）、人員名（清單內人員為正規鍵；**「其他」**時標題僅顯示 **「其他」**，該筆 `rawName` 人名改在左右欄 `event-card` **最上方**顯示，見 §10.8）、類型標籤（新增／刪除／修改）。  
  - 內容區固定 **左右兩欄**：**左＝上一版**、**右＝新版**（中間可選分隔線）。  
  - **修改**：左欄上一版 `event-card`（灰底），右欄新版 `event-card`（正常）。  
  - **刪除**：左欄上一版卡；右欄空欄顯示 **「無」**（見 §10.3.1）。  
  - **新增**：左欄空欄顯示 **「無」**；右欄新版卡。  
  - 排序：**日期由早到晚** → **人員**（見下）→ **同日同人**內 **刪除 → 修改 → 新增**（與 §10.6.2 產出順序一致）。**同日不同人**依**表頭順序**（見 §10.8「變更卡列表排序」）。**同日同人多筆不合併**（一筆事件級變更一張變更卡）。  
  - 變更卡內左右欄 `event-card`：**不**受檢視 dialog「顯示地點／期別／別名」等開關影響；依事件資料**完整**顯示專案、地點、期別及適用之 `rawName`（規則同月曆卡片內文，但**不**套用檢視 sheet 核取方塊）。月曆本體仍受檢視開關控制。  

#### 10.3.1 變更卡版面（左右對照，已定案）

變更卡（dialog 全體列表與 §10.5 `#person-diff-panel`）採**同一套左右欄**，不採上下堆疊：

```
┌─────────────────────────────────────────────┐
│ 5/20（二）· 維鈞                      [修改] │
├──────────────────┬──────────────────────────┤
│ 上一版（左）      │ 新版（右）                │
│ ┌ event-card ┐   │ ┌ event-card ┐           │
│ │ 灰底樣式   │   │ │ 正常樣式   │           │
│ └────────────┘   │ └────────────┘           │
└──────────────────┴──────────────────────────┘
```


| 項目     | 規格                                                                                                                           |
| ------ | ---------------------------------------------------------------------------------------------------------------------------- |
| 欄位標題   | 各欄上方小字：**上一版**（左）、**新版**（右）；或僅在修改類型顯示，刪除／新增時依空欄規則省略對側標題                                                                      |
| 欄寬     | 預設各 **50%**（`display: grid; grid-template-columns: 1fr 1fr` 或 flex 均分）；欄間 `gap` + 可選 `border-left` 分隔                        |
| DOM 建議 | 外層 `.change-card` → `.change-card__cols` → `.change-card__col.change-card__col--baseline`（左）、`.change-card__col--current`（右） |
| 僅刪除    | 左欄放 ghost／灰底上一版卡；右欄 `.change-card__col--empty` 顯示 **「無」**                                                                      |
| 僅新增    | 左欄 `.change-card__col--empty` 顯示 **「無」**；右欄放新版卡                                                                                                              |
| 卡片內容   | **不**連動檢視 sheet 之「顯示地點／期別／別名」等；變更卡內 event-card 一律依事件完整欄位渲染（見 §10.3 狀態 C）                                                                 |
| 響應式    | 極窄視窗（如 `max-width: 360px`）改為上下堆疊（上＝上一版、下＝新版），但**預設與桌面均以左右為準**；實作時以媒體查詢切換，產品預期仍以左右對照為主                                        |


> **與月曆差異的區別**：§10.6 月曆格子內仍只渲染**新版**事件卡（加 diff 邊框／ghost）；**不**在月曆日格內做左右雙欄。左右對照**僅**用於變更卡列表（dialog 與 `#person-diff-panel`）。

#### 狀態 D：解析失敗

- Dialog 內顯示錯誤訊息（風格同 `#error-banner`），不關 dialog。
- **重新選擇上一版**（或首次匯入上一版）若解析失敗：**維持**原 `baselineParsed`、檔名與已算 diff（與 §10.9 匯入新版失敗對稱）；僅顯示錯誤。

**關閉 dialog 後**：保留 `baselineParsed` 與已計算之 diff 結果（記憶體；是否 `sessionStorage` 持久化列為可選，本階段不強制）。若「檢視 → 顯示與上一版差異」為勾選，立即重繪月曆。

### 10.4 「檢視」Dialog：顯示差異開關


| 項目         | 規格                                                                |
| ---------- | ----------------------------------------------------------------- |
| 區塊         | 新增小節 **【與上一版比較】**                                                 |
| 控制項        | Checkbox 文案：**顯示與上一版差異**；`id` 建議 `#view-show-baseline-diff`；**預設勾選** |
| 尚未匯入提示     | 當 `baselineParsed === null` 時，於**同一 label** 內、勾選框文案**後方**以灰字顯示 **（尚未匯入上一版，請至底欄「比較」匯入）**（不另開主內容區橫幅） |
| 勾選且已有上一版   | 月曆＝新版內容 ＋ 差異標示（§10.6）                                             |
| 勾選但尚未匯入上一版 | 月曆同現況（僅新版）；無 diff UI；僅顯示上述 label 內灰字提示                                |
| 未勾選        | 月曆**完全等同現況**（只畫 `parsed`，無 diff UI）；不顯示「尚未匯入」灰字（與勾選狀態連動）          |
| 行為         | `change` → `renderCalendar()`；必要時更新圖例                             |


### 10.5 主內容區：圖例與第一個月曆之間（目前人員）

當 **已匯入上一版** 且 **「顯示與上一版差異」勾選** 時，於 `#legend` 與**第一個**月曆區塊（`#calendar-root` 內第一個 `.month-section`）之間插入 **目前選取人員** 專用之區塊（建議 `#person-diff-panel`）：


| 區塊        | 範圍                       | 說明                                                         |
| --------- | ------------------------ | ---------------------------------------------------------- |
| **摘要卡**   | 僅**目前人員**（含選「其他」時僅「其他」桶） | 口徑同 dialog，但**不顯示「M 位人員」**（見 §10.8）；例：`共 N 處變更`；`K 個日期`；`新增 a · 刪除 b · 修改 c`（N、a/b/c、K 皆僅限該員） |
| **變更卡列表** | 同上                       | 與 dialog **相同左右對照 UI**（§10.3.1），但只列出該員（「其他」依 `rawName` 區分） |


- 切換人員（底欄人員 dialog）→ 更新此區塊並重繪月曆 diff 標示。  
- 未勾選「顯示與上一版差異」或無上一版 → **不渲染**此區塊。  
- 該員無任何變更時：可顯示「與上一版相比，{人員名} 無變更」。

**與 dialog 分工**：dialog 看**全體**（含僅上一版存在之人員，見 §10.1）；主內容區看**當前選取人員**（僅底欄可選之新版 `people` +「其他」），方便對照月曆捲動查閱。

### 10.6 月曆差異顯示（方案 2 ＋ 1b，已定案）

前提：格子內**仍以新版事件為主**；僅在「顯示與上一版差異」開啟且存在 `baselineParsed` 時套用。

**與檢視 sheet 連動（已定案）**：月曆上的班別卡（含 **Ghost 刪除卡**，見下）仍受「顯示地點／期別／別名／**顯示同天同專案的人員**」等既有核取方塊控制（預設行為與現況相同；Ghost 卡經同一套 `fillEventCard` 渲染，關閉地點／期別等時同樣隱藏對應行）。**變更卡**內 event-card 則不連動（見 §10.3）。

#### 10.6.1 方案 1b：日期格角標＋一行摘要

對**當前人員**、**當月 grid 內**且**有 diff** 之 `day-cell`（含 §10.7 僅刪除日）：


| 變更類型 | 角標（`date-num` 旁） | 摘要行（`.day-diff-summary`，極小字，在角標下、事件卡上） |
| ---- | ---------------- | -------------------------------------- |
| 僅新增  | 綠 `+`            | 例：`1 增`                                |
| 僅刪除  | 紅 `−`            | 例：`此日已無排班（上一版 N 班）`                    |
| 僅修改  | 橙 `~`            | 固定簡短文案，例：`有修改`（**不**逐項列「1 刪 · 1 改」） |
| 混合   | 橙 `±`            | 固定簡短文案，例：`有變更`（**不**使用「1 增 · 1 刪 · 1 改」等逐項合併文案）；含「新增＋修改（無刪）」「刪除＋修改（無增）」等 |


**日格角標優先**（當日事件級，對**當前人員**）：若**僅**有修改 → **`~`**；若**僅**有新增 → **`+`**；若**僅**有刪除 → **`−`**；**其餘**（含增＋刪、增＋改、刪＋改、增＋刪＋改等，只要不是上述三種「僅」）→ **`±`**，摘要 **「有變更」**。

`day-cell` 可加淡底 class（如 `day-cell--diff-add|remove|change|mixed`），與 `.today` 並存時需注意對比。

#### 10.6.2 方案 2：事件卡層級


| 類型            | 新版卡                                                                    | 上一版僅刪除                                                                                        |
| ------------- | ---------------------------------------------------------------------- | --------------------------------------------------------------------------------------------- |
| **新增**        | 左側綠邊 `event-card--diff-add`                                            | —                                                                                             |
| **修改**        | 左側橙邊 `event-card--diff-chg` ＋ 卡底一行小字 `**原：{專案} · {地點}`**（期別若有則併入；**不含** `rawName`；單行省略） | —                                                                                             |
| **刪除**（新版無對應） | —                                                                      | **Ghost 卡** `event-card--diff-remove`：灰底、虛線框、刪除線專案名、`opacity` 略降；文案 **刪除（上一版）**；排在該日**新版卡下方**；內文欄位（地點／期別／`rawName` 等）**連動檢視 sheet**（與一般月曆卡相同，見上） |
| **未變**        | 維持現有專案色，無 diff class                                                   | —                                                                                             |


**事件對應規則**（增刪改判定，實作須一致；詳見 **§10.12**）：

1. 同人同日，以 `(project, location, period, rawName)` 正規化字串（空值統一）組 **multiset** 比對。
2. 四元組完全一致 → **未變**。
3. 僅新版有 → **新增**；僅上一版有 → **刪除**（ghost）。
4. 精確 multiset 後，於**剩餘**事件中：將 **專案名稱字串完全一致**（不做 fuzzy；任一字元不同即視為不同專案）者 **一對一** 配為 **修改**（新版卡 + `原：…`）。
  - 例：兩版各 2 筆**同專案**、地點／期別皆不同 → **2 筆修改**（非 2 刪 + 2 增）。
  - 例：刪專案 `A`、增專案 `B`，或刪 `A`、增 `A'`（字串不同）→ **刪 + 增**（不算修改）。
  - **配對順序**：同一專案之多筆 unmatched，依**解析順序**（專案區塊內欄位**由左到右**）第 1 對第 1、第 2 對第 2……
5. **僅 `rawName` 不同**（專案、地點、期別皆同）：因比對 key 含 `rawName`， multiset 視為不同事件 → **刪除 + 新增**，**不算**修改；月曆 `原：…` 亦**不**為此情境使用。
6. **清單內人員 ↔「其他」**（人員桶變更）：一律 **刪除 + 新增** 各一張變更卡（標題為一般「刪除」「新增」；清單內人員標題為正規鍵，「其他」標題為「其他」且 `rawName` 在卡內最上；**不**另立類型）。

#### 10.6.3 圖例（`#legend`）

在專案色圖例**下方**追加 diff 圖例列（與專案 dot 形狀或標籤區分）：

- **新增**（綠邊示例）  
- **修改**（橙邊 + 「原：…」示例）  
- **刪除（上一版）**（ghost 卡示例）

僅在顯示差異模式且已有上一版時顯示。

### 10.7 僅刪除、新版整格為空的日子（已定案：**要**出現在月曆 grid）

- **必須**在月曆中畫出該 `day-cell`（即使新版 `byPerson[person][date]` 無事件）。  
- 該格僅顯示：角標 `−`、摘要「此日已無排班…」、**ghost 刪除卡**（上一版事件）。  
- **月份收集**：在「顯示與上一版差異」模式下，`collectMonths`（或同等函式）須合併 **新版** 與 **上一版** 對**當前人員**有事件或 diff 之日期所屬曆月，避免僅因新版無資料而整月消失。  
- **有資料月定義（diff 模式）**：僅因上一版有班／僅刪除 diff 而需顯示的曆月，**仍算**有資料月（納入 `collectMonths`）。  
- 與「只顯示本月及未來月份」連動：若該刪除日落在被篩掉之過去月份，則不顯示（與現有月份篩選一致）。  
- 與「顯示無資料月份」：幽靈月**僅**插在**畫面上相鄰**的兩個有資料月之間（與第 5 節規則 3 一致）。例：6 月被月份篩選隱藏、畫面僅剩 9 月時，**不**在 9 月上方補 7、8 月幽靈區塊；6 月與 9 月**皆**在畫面中時，中間仍依規則插入 7、8 月。  
- **不因新版無事件而跳過**有 diff 之曆月（該月以 `buildMonth` 等方式完整畫出 grid）。

### 10.8 Diff 類型定義（語意層）


| 類型     | 定義                                              |
| ------ | ----------------------------------------------- |
| **新增** | 上一版無、新版有之事件（§10.6.2 步驟 3）                       |
| **刪除** | 上一版有、新版無（§10.6.2 步驟 3）                                        |
| **修改** | §10.6.2 步驟 4 配對成功；專案字串完全一致且地點或期別不同；**不含**僅 `rawName` 不同 |


**「其他」**：比對 key 須含 `rawName`；不同 `rawName` 視不同事件。變更卡**標題列**人員欄固定顯示 **「其他」**；該筆 **`rawName` 人名**在左右欄 `event-card` **最上方**顯示（與選「其他」月曆卡之人名置頂一致）。人員桶在「其他」與清單內人員之間移動 → **刪除 + 新增**（§10.6.2 步驟 6）。

**摘要統計**（dialog 全體摘要卡 vs `#person-diff-panel` 目前人員摘要卡）：


| 符號 | 定義 |
| ---- | ---- |
| **a / b / c** | 事件級**新增／刪除／修改**筆數；與變更卡類型、月曆角標**同一套**分類 |
| **N** | **事件級變更總數** = `a + b + c`（正常情況下等於變更卡列表張數；若兩者不一致，**N 仍以事件級為準**） |
| **M** | 至少有一筆增／刪／改的人數（以 §10.1 **兩版 `byPerson` 聯集** 為準）；**僅在上一版有變更**的人也計入；**僅**用於比較 dialog 全體摘要 |
| **K** | 有 diff 的日期去重數；dialog 為**全體**（跨人員）；`#person-diff-panel` 為**當前人員** |

- **比較 dialog（全體）** 例：`共 N 處變更`；`M 位人員 · K 個日期`；`新增 a · 刪除 b · 修改 c`。
- **`#person-diff-panel`（目前人員）**：**不顯示 M**；例：`共 N 處變更`；`K 個日期`；`新增 a · 刪除 b · 修改 c`（N、a/b/c、K 皆僅限該員）。

**變更卡列表排序（已定案）**（dialog 全體列表與 `#person-diff-panel` 同規則；後者僅篩同一員）：

1. **日期**由早到晚；
2. **人員**：先依**新版** `parsed.people` 表頭順序；再依**僅出現在上一版**、不在新版 `people` 者，以**上一版** `baselineParsed.people` 表頭順序接續；最後為**「其他」**（與人員 dialog 一致，固定尾端）；
3. **同日、同為「其他」桶、不同 `rawName`**：依 `rawName` 字串 **`localeCompare`（Unicode／預設語系）** 由小到大；
4. **同日同人**（含清單內人員；「其他」且 `rawName` 相同者視同人）：**刪除 → 修改 → 新增**。

### 10.9 與其他流程的銜接


| 情境                | 行為                                                      |
| ----------------- | ------------------------------------------------------- |
| 頂部重新匯入新版（成功）      | 保留上一版與檔名；**重算** diff；更新 dialog／`#person-diff-panel`／月曆  |
| 頂部重新匯入新版（**失敗**）  | **維持**原 `parsed` 與畫面；**不清除** `baselineParsed`、檔名與已算 diff；顯示錯誤（風格同 `#error-banner`） |
| 比較 dialog 重新選擇上一版（**成功**） | 覆寫 `baselineParsed` 與檔名；重算 diff                         |
| 比較 dialog 重新選擇上一版（**失敗**） | **維持**原 `baselineParsed`、檔名與已算 diff；Dialog 內顯示錯誤   |
| 關閉「顯示與上一版差異」      | 隱藏 `#person-diff-panel`、diff 圖例、月曆 diff 樣式；月曆月份列表回到僅依新版 |
| 切換人員              | 更新 `#person-diff-panel`；月曆 diff 僅反映該員                   |
| 無上一版              | 比較按鈕可開 dialog；主內容區無 person-diff；月曆無 diff                |


**持久化**：本階段不強制 `sessionStorage`；刷新頁面後需重新匯入兩版（可列為後續增強）。

### 10.10 建議模組與檔案（實作時）


| 檔案                                           | 職責                                                                                                      |
| -------------------------------------------- | ------------------------------------------------------------------------------------------------------- |
| `src/compareSchedules.js`（新建，建議）             | `compareSchedules(baseline, current) → { summary, changesByPerson, dayDiffByPerson, sortedChanges }`；人員 key 取兩版 `byPerson` **聯集**；事件 multiset／配對；變更卡排序見 §10.8 |
| `src/main.js`                                | `#dialog-compare`、導覽第四鍵、`#person-diff-panel` 渲染、`buildMonth` diff 標示、`collectMonths` 合併邏輯               |
| `index.html`                                 | `#dialog-compare`、檔案 input、`#view-show-baseline-diff`                                                   |
| `styles.css`                                 | `.dialog--compare`、`.change-card` 左右欄（§10.3.1）、`.day-diff-summary`、`event-card--diff-`*、ghost 卡、diff 圖例 |
| `[ui_develop_plan.md](./ui_develop_plan.md)` | 底欄四鍵、Phase「更多」旁比較入口（實作時同步修訂）                                                                            |


### 10.11 測試計劃（建議）


| 案例                | 預期                                    |
| ----------------- | ------------------------------------- |
| 未匯入新版即開比較         | 無法選上一版或提示先匯入新版                        |
| 匯入上一版後 dialog     | 全體摘要＋變更卡正確；修改類為**左上一版、右新版**；0 差異時文案正確 |
| 檢視取消「顯示與上一版差異」    | 月曆與現版一致；無 `#person-diff-panel`        |
| 目前人員摘要區           | 僅顯示該員變更；摘要**不含**「M 位人員」；切換人員後更新          |
| Ghost 卡 × 檢視開關    | 關閉「顯示地點／期別」等時，月曆 Ghost 刪除卡亦隱藏對應行（與一般卡一致） |
| 新增日               | 綠邊卡 + 角標                              |
| 修改日               | 橙邊 + `原：…`                            |
| 僅刪除日（新版空格）        | grid 仍有該日；ghost 卡 + 角標 + 摘要           |
| 僅刪除日所在月（新版該月無任何日） | 該月仍出現在月曆序列（diff 模式）                   |
| 選「其他」             | `rawName` 區分；與清單內人員不混淆                |
| 重新匯入新版／上一版        | diff 重算正確                             |
| 重新匯入新版失敗           | 仍為舊 `parsed`；`baselineParsed` 與 diff 保留；錯誤提示可見 |
| 重新選擇上一版失敗           | 仍為舊 `baselineParsed` 與 diff；錯誤提示可見                 |
| 尚未匯入上一版、勾選顯示差異     | 檢視 label 內灰字「尚未匯入…」；月曆無 diff                      |
| 「其他」變更卡標題           | 標題為「其他」；`rawName` 在卡內最上                             |
| 同日「其他」多 rawName      | 變更卡依 `rawName` `localeCompare` 排序                      |
| 僅上一版存在之人員           | 變更出現在比較 dialog；底欄人員不可選、月曆無該員 diff |
| 同日不同人變更卡排序         | 依新版表頭順序（僅上一版者接續上一版表頭順序）；「其他」最末 |
| 月份篩選 × diff       | 過去月不顯示含僅刪除日在內之 diff（若該日被篩掉）           |
| 同專案 2 改 2 改       | 兩筆**修改**變更卡；配對依欄位由左到右                       |
| 專案字串略不同           | **刪 + 增**，不算修改                                 |
| 僅 rawName 變        | **刪 + 增**，不算修改；無 `原：…`                         |
| 同日 1 增 1 刪 1 改     | **3 張**變更卡；日格角標 **`±`**；摘要不列「1 增 · 1 刪 · 1 改」 |
| 同日 1 增 1 改（無刪）    | 日格角標 **`±`**、摘要「有變更」                              |
| 同日 1 刪 1 改（無增）    | 日格角標 **`±`**、摘要「有變更」                              |
| 比較 dialog 零差異（N=0） | 摘要 `共 0 處變更` 等；變更卡區「兩版相同，無變更」；檔名列仍在          |
| 清單內 ↔「其他」         | 各一張「刪除」「新增」變更卡                                 |
| 6 月隱藏、9 月有 diff    | 9 月上方**不**出現 7、8 月幽靈月                          |


### 10.12 實作定案（2026-05-17）

本節彙整產品確認之演算法與 UI 口徑；與 §10.3、§10.6、§10.8 衝突時以本節為準。

1. **專案名稱**：僅字串**完全一致**可配為修改；不做 fuzzy。
2. **修改配對**：multiset 精確比對後，剩餘同專案事件一對一；多筆同專案 → 多筆修改；配對順序 = 解析欄位**由左到右**（第 1 對第 1…）。
3. **不算修改**：專案字串不同 → 刪 + 增；**僅 `rawName` 不同**（專案／地點／期別同）→ 刪 + 增。
4. **變更卡**：不合併；排序見 §10.8「變更卡列表排序」（日期 → 表頭人員順序 → 同日「其他」依 `rawName` `localeCompare` → 同日同人刪→改→增）。
5. **摘要 N/M/K/a/b/c**：見 §10.8 表；比對人員 = 兩版 `byPerson` **聯集**；`#person-diff-panel` **不顯示 M**。
6. **月曆角標**：見 §10.6.1（僅增／僅刪／僅改 → `+`／`−`／`~`；其餘混合 → `±`）；混合日摘要用固定短句，不逐項列增刪改數。
7. **幽靈月**：diff 納入之月算有資料月；僅在**畫面上相鄰**有資料月之間插入（見 §10.7）。
8. **人員桶變更**：刪 + 增兩張一般變更卡。
9. **僅上一版人員**：僅比較 dialog 全體列表可見；不可切人員看月曆（產品接受）。
10. **變更卡內容**：**不**連動檢視 sheet 顯示開關；空欄文案固定 **「無」**。
11. **重新匯入新版失敗**：維持舊 `parsed`；**不**清 `baselineParsed`。
12. **匯入／重選上一版失敗**：維持舊 `baselineParsed` 與 diff（與第 11 點對稱）。
13. **「其他」變更卡**：標題僅「其他」；`rawName` 在 event-card 最上。
14. **同日「其他」多 `rawName`**：以 `rawName` **`localeCompare`** 排序。
15. **尚未匯入上一版提示**：寫在「顯示與上一版差異」checkbox **文案後方**灰字。
16. **月曆 diff**：同專案 peers 行仍受既有檢視 checkbox 控制；**Ghost 刪除卡**亦經 `fillEventCard`，受「顯示地點／期別／別名」等開關控制（與一般月曆卡相同）。
17. **清除上一版**：Phase 1 **不做**專用按鈕。
18. **`#person-diff-panel` 摘要**：**不顯示**「M 位人員」；僅 `N`、`K`、`a/b/c`（皆限當前人員）。
19. **比較 dialog 零差異**（`N === 0`）：仍顯示檔名列／重新選擇與摘要（`共 0 處變更` 等）；變更卡區一行 **「兩版相同，無變更」**。
20. **日格混合角標**：非「僅增／僅刪／僅改」一律 **`±`**（含增＋改、刪＋改）。

### 10.13 修訂紀錄


| 日期         | 說明                                                           |
| ---------- | ------------------------------------------------------------ |
| 2026-05-17 | 初版：比較功能 UI／行為定案；月曆採方案 2＋1b；主內容區目前人員摘要／變更卡；僅刪除日仍畫入 grid；待實作   |
| 2026-05-17 | 變更卡改為**左右對照**（左＝上一版、右＝新版）；§10.1 用途欄與 §10.3.1 版面規格；月曆日格不做左右雙欄 |
| 2026-05-17 | **§10.12 實作定案**：專案嚴格比對、修改配對順序（欄左→右）、變更卡不合併、摘要口徑、僅 rawName 變不算修改、日格 `±`／混合摘要、幽靈月與 diff 有資料月、清單內↔「其他」為刪+增 |
| 2026-05-17 | 產品確認：比對範圍為兩版 `byPerson` 聯集；僅上一版人員僅 dialog 可見；變更卡排序含同日依表頭順序；變更卡不連動檢視開關；空欄「無」；匯入新版失敗保留舊版與 baseline |
| 2026-05-17 | 產品確認：匯入上一版失敗保留 baseline；「其他」同日依 `rawName` localeCompare；尚未匯入提示在 checkbox 文案後；「其他」人名在卡內最上；月曆 peers 仍連動檢視開關；Phase 1 無「清除上一版」 |
| 2026-05-17 | 產品確認：`#person-diff-panel` 摘要不顯示「M 位人員」；月曆 Ghost 刪除卡連動檢視 sheet（地點／期別／別名等，與一般班別卡相同） |
| 2026-05-18 | §4.5 標為已實作（`resolvePersonNames.js`）；日格非「僅增／僅刪／僅改」一律 `±`；比較 dialog 零差異：摘要仍顯示、變更卡區「兩版相同，無變更」 |


---

## 11. 預設載入最新日期總排班表（規格，待實作）

> **狀態**：產品方向已定；**尚未開發**。實作前以本節為準。  
> **與既有匯入的關係**：頂部「匯入總排班表」**保留**；使用者仍可手動選檔覆寫目前顯示之新版。本節**不**涵蓋上一版（baseline）預設匯入。

### 11.1 目標與動機

- **目標**：開啟網站（`npm run dev` 或部署後之 GitHub Pages）時，若專案內已放置符合命名規則的 xlsx，**自動**載入**檔名日期最新**的一份作為新版總排班表，無需每次手動點匯入。
- **動機**：營運方以固定流程更新總表；檔名帶版本日期，網站應預設呈現最新一版。
- **範圍外（Phase 1）**：
  - **不**自動匯入上一版（`baselineParsed` 仍僅能透過「比較」dialog 手動選檔，見第 10 節）。
  - **不**嘗試讀取使用者本機任意資料夾（瀏覽器安全限制；見 §11.2）。

### 11.2 技術前提（為何必須走 `public/` + manifest）

| 限制 | 說明 |
| --- | --- |
| 無法掃描本機目錄 | 純前端網頁**不能**列出 `~/Documents/schedules/` 等路徑下的檔案；僅能透過 HTTP `fetch` 取得**已隨站點部署**的靜態 URL，或使用者以 `<input type=file>` 授權的單檔。 |
| 無法列靜態目錄 | 即使 xlsx 放在 `public/schedules/`，瀏覽器也**不能**對 `GET /schedules/` 取得目錄清單（一般靜態主機不開 directory listing）。因此需 **`manifest.json`** 記錄有哪些檔案。 |
| 部署路徑 | 專案以 Vite 建置，`base` 在本機 dev 為 `/`，GitHub Pages 為 `/DrivingInstructorShiftSchedule/`（見 `vite.config.js`）。`fetch` 路徑須使用 **`import.meta.env.BASE_URL`** 前綴，避免 Pages 子路徑 404。 |
| 禁止 `file://` | 直接雙擊開 `index.html` 通常無法可靠 `fetch` 同目錄資源；須 `npm run dev` 或 `vite preview`／靜態主機。 |

### 11.3 目錄與檔案約定

```
public/
  schedules/
    shift-20250515.xlsx    # 範例：2025-05-15 版
    shift-20250518.xlsx    # 範例：2025-05-18 版（較新）
    manifest.json          # 建置前由腳本產生，勿手改（見 §11.4）
```

| 項目 | 規格 |
| --- | --- |
| xlsx 放置位置 | `public/schedules/`（Vite 原樣複製至輸出根目錄；`npm run build` 後出現在 `docs/schedules/`） |
| 檔名格式 | **`shift-YYYYMMDD.xlsx`**：固定前綴 `shift-` ＋ **8 位西元日期** ＋ `.xlsx`（大小寫：前綴與副檔名建議小寫；實作時以 **case-insensitive** 比對副檔名即可） |
| 檔名正則（建議） | `^shift-(\d{8})\.xlsx$`（`i` 修飾子可選，用於副檔名） |
| 日期語意 | 檔名中的 `YYYYMMDD` 僅用於**挑選哪一份檔案要載入**，**不**用於推斷班表內各列的日期（班表日期仍以 Excel 儲存格解析為準，與第 8 節風險第 6 點一致） |
| 不符合檔名者 | `public/schedules/` 內任一 `.xlsx` 若**不符合** §11.3 正則 → **`npm run build`／`schedules:manifest` 失敗**（exit code `1`），終端機列出**每一個**問題檔名與原因（見 §11.4.4） |
| 無效日期 | 8 位數字但非合法曆日（如 `shift-20250230.xlsx`）→ 同上，**fail build** 並標明無效日期 |
| 空目錄 | 允許 `public/schedules/` **沒有任何** `.xlsx`（僅 `manifest.json` 且 `files: []`）；啟動時視同「無預設檔」，維持現有空狀態（見 §11.6） |
| 版控 | **`public/schedules/*.xlsx` 與 `manifest.json` 皆納入 git**（見 §11.7）；部署後瀏覽器才能 `fetch` 預設檔 |

**同日期多檔（邊界）**：若存在兩份皆符合正則且 `date` 相同（理論上不應發生）→ 建置腳本 **fail build** 並列出衝突檔名。若存在 `shift-20250518-a.xlsx` 等不符合正則者 → **fail build**（不略過）。未來允許同日期多檔不在本 Phase 範圍——目前規格為**一日期一檔名**。

### 11.4 建置時產生 `manifest.json`

#### 11.4.1 腳本

- 新增 Node 腳本（建議路徑）：`scripts/generate-schedules-manifest.mjs`
- 行為：
  1. 掃描 `public/schedules/*.xlsx`（僅此層，不遞迴子資料夾）。
  2. 列出 `public/schedules/*.xlsx`；對**每一個**檔案驗證 §11.3 正則與日期合法性；任一不合格 → **不寫 manifest**、**`process.exit(1)`**（見 §11.4.4）。
  3. 檢查 `date` 是否重複；若重複 → fail build 並列出衝突檔名。
  4. 依 `date`（`YYYY-MM-DD`）**由新到舊**排序。
  5. 寫入 `public/schedules/manifest.json`（UTF-8、縮排 2 空格）。

#### 11.4.2 `manifest.json` 結構（version 1）

```json
{
  "version": 1,
  "generatedAt": "2026-05-18T08:30:00.000Z",
  "files": [
    {
      "name": "shift-20250518.xlsx",
      "date": "2025-05-18"
    },
    {
      "name": "shift-20250515.xlsx",
      "date": "2025-05-15"
    }
  ],
  "latest": "shift-20250518.xlsx"
}
```

| 欄位 | 說明 |
| --- | --- |
| `version` | 固定 `1`；日後結構變更可遞增 |
| `generatedAt` | ISO 8601；腳本執行時間 |
| `files` | 已納入之檔案；**已按 `date` 降序** |
| `latest` | `files[0].name`；無檔時為 `null` 或省略（實作擇一並於前端統一判斷） |

#### 11.4.3 npm 腳本掛鉤

| 指令 | 行為 |
| --- | --- |
| `schedules:manifest`（建議） | 僅執行 manifest 產生腳本 |
| `prebuild` | 於 `vite build` **之前**執行 `schedules:manifest`（滿足「`npm run build` 前掃描並產生 manifest」） |
| `predev`（建議一併） | 於 `vite` dev **之前**執行 `schedules:manifest`，避免本機開發時忘記產生 manifest 而預設載入失敗 |

> **版控（已定案）**：`manifest.json` 與 `public/schedules/*.xlsx` **皆 commit 進 repo**。新增／更名 xlsx 後須執行 `schedules:manifest`（或 `prebuild`／`predev`）並一併 commit 更新後的 manifest。

#### 11.4.4 建置失敗與錯誤訊息（已定案：fail build）

任一 `public/schedules/*.xlsx` 驗證失敗時，腳本**必須**以 **exit code `1`** 結束，使 `npm run build`／`predev` 中斷；**不得**僅 warning 後略過問題檔。

**終端機輸出格式（範例）**——須能一眼看出**哪個檔名**、**哪一條規則**未過：

```
✖ public/schedules/：排班表檔名驗證失敗（共 2 個）

  other.xlsx
    預期格式：shift-YYYYMMDD.xlsx（例：shift-20250518.xlsx）

  shift-20250230.xlsx
    檔名日期無效：20250230（非合法曆日）

請重新命名或移出 public/schedules/ 後再執行 npm run schedules:manifest
```

| 失敗原因 | 建議訊息要點 |
| --- | --- |
| 不符合正則 | 標示**實際檔名** + 預期格式範例 |
| 8 位日期非法曆日 | 標示擷取出的 `YYYYMMDD` |
| 同 `date` 多檔 | 列出所有衝突檔名（例：`shift-20250518.xlsx` 與 `shift-20250518-copy.xlsx` 若皆合法則不會發生；兩檔同 date 才觸發） |

**實作備註**：Node 腳本可一次收集所有不合格檔再統一印出（避免修一個才發現下一個）。若實作上無法保證每檔皆有明確原因字串，則降級為 warning（**本專案以 fail + 逐檔說明為準**）。

### 11.5 前端啟動流程

#### 11.5.1 時機

- 於 `src/main.js` 現有初始化（`fillPersonRadios()`、`renderEmptyHint()` 等）**之後**（或改為 await 預設載入完成再渲染，避免閃爍——實作時擇一，見 §11.6）。
- **每次**頁面載入執行一次；**不**寫入 `localStorage` 記住上次手動匯入檔名（避免與「永遠最新 manifest」衝突）。

#### 11.5.2 演算法

1. `const base = import.meta.env.BASE_URL`（確保尾端有 `/` 或自行正規化）。
2. `GET ${base}schedules/manifest.json`  
   - 404／網路錯誤 → 進入 §11.6 失敗路徑。  
   - JSON 解析失敗 → 失敗路徑。
3. 若 `!latest` 且 `files.length === 0` → **不載入**，維持空狀態。
4. `GET ${base}schedules/${latest}` → `arrayBuffer()`。
5. 呼叫與手動匯入相同之解析管線：`parseShiftWorkbook(buf)` → 更新 `parsed` → `recomputeCompare()`、`rebuildProjectClassMap()`、`fillPersonRadios()`、`fillLegend()`、`renderCalendar()`、`updatePersonSummary()`、`refreshDiffUi()`（與現有 `onFile` 成功分支一致）。
6. 建議抽出 `loadScheduleFromArrayBuffer(buf, { fileName, source: 'auto' | 'manual' })`，供 `onFile` 與預設載入共用；成功後更新 §11.5.4 之「目前排班表」UI。

#### 11.5.3 與手動匯入、比較功能

| 情境 | 行為 |
| --- | --- |
| 預設載入成功後，使用者再點「匯入總排班表」 | 以新檔**覆寫** `parsed`（現行 `onFile` 行為不變） |
| 預設載入失敗 | 仍可手動匯入；**不**禁用匯入按鈕 |
| 已匯入 `baselineParsed` 後，僅重新整理頁面 | baseline **清空**（與現況一致）；預設載入僅影響新版 `parsed` |
| 上一版 baseline | **無**預設路徑、**無**自動 `fetch` |

#### 11.5.4 目前排班表檔名（UI，已定案）

**目標**：成功載入新版總排班表後（**自動**或**手動匯入**），header 內**一律**顯示目前使用中的檔名，避免使用者不知道開的是哪一版。

**位置**：`index.html` 的 `.toolbar` 內、匯入按鈕**下方**（仍屬 header、隨頁面捲動，不 sticky）。與 `#person-summary` 分工：人員摘要在其下、字級較大；檔名列較小、偏輔助資訊。

**版面示意（直向手機）**：

```
┌─────────────────────────────┐
│ 總排班表 · 個人月曆            │
│ 檔案僅在瀏覽器解析…            │
│  [ 匯入總排班表 ]              │
│  目前排班表：shift-20250518.xlsx  [自動]   │  ← 本節新增
│  人員：維鈞                   │  ← 既有 #person-summary
└─────────────────────────────┘
```

**DOM（建議）**：

```html
<p id="schedule-source" class="schedule-source" hidden>
  <span class="schedule-source__label">目前排班表：</span>
  <span class="schedule-source__name" id="schedule-source-name"></span>
  <span class="schedule-source__badge" id="schedule-source-badge" hidden></span>
</p>
```

| 元素 | 規格 |
| --- | --- |
| `#schedule-source` | 無資料時 `hidden`；有 `parsed` 時顯示 |
| `.schedule-source__label` | 固定文案「目前排班表：」 |
| `#schedule-source-name` | 檔名字串；`title` 屬性同全文（長檔名 hover 可看全） |
| `#schedule-source-badge` | 僅在 `source === 'auto'` 時顯示，文案 **「自動」**；手動匯入後**隱藏** badge（或改文案「本機」，Phase 1 **建議僅「自動」／無 badge」二態**，較簡潔） |

**狀態與文案**：

| 狀態 | `#schedule-source` | 檔名 | Badge |
| --- | --- | --- | --- |
| 尚未載入 | hidden | — | — |
| 預設 `fetch` 成功 | 顯示 | manifest 的 `latest`（例 `shift-20250518.xlsx`） | **自動** |
| 手動匯入成功 | 顯示 | `File.name`（使用者選的檔名，**不**強制符合 `shift-*`） | 無 |
| 手動匯入覆寫先前自動載入 | 顯示 | 新檔名 | 無（badge 移除） |
| 解析失敗／清空 `parsed` | hidden | — | — |

**樣式（`styles.css`，對齊既有 subtitle／toolbar）**：

| 項目 | 規格 |
| --- | --- |
| 容器 `.schedule-source` | `width: 100%`；`margin: 0`；`padding: 0 4px`；`font-size: 0.85rem`；`color: #666`；`line-height: 1.4`；置中（與 `.toolbar` 一致） |
| 檔名 `.schedule-source__name` | `color: #444`；`font-weight: 600`；`word-break: break-all`（極長檔名換行）；窄螢幕可選 `max-width` + `ellipsis`（與 `.person-summary__text` 類似） |
| Badge `.schedule-source__badge` | 緊接檔名後；`font-size: 0.75rem`；`font-weight: 600`；`padding: 2px 8px`；`border-radius: 6px`；`background: rgba(26, 26, 46, 0.08)`；`color: #1a1a2e`；`letter-spacing: 0.04em`；與 `.person-summary` 同色系、但更輕量 |
| 與錯誤區分 | **不**使用 `.error-banner` 紅底；成功資訊僅在此列；失敗仍走 `#error-banner` |

**無障礙**：`#schedule-source` 在顯示時可設 `aria-live="polite"`，匯入成功後螢幕報讀會更新檔名（可選）。

**與比較功能一致感**：比較 dialog 已有 `上一版：{檔名}`；本列為新版之對稱資訊，固定放在 header 常駐可見。

#### 11.5.5 其他 UI 回饋

- **預設載入失敗**（manifest 空、404、解析錯誤）：`#schedule-source` 維持 hidden；`#error-banner` 顯示，例：`無法載入預設排班表，請手動匯入` 或解析錯誤原文；**不**禁用匯入按鈕。

### 11.6 失敗與空狀態

| 情況 | 預期 |
| --- | --- |
| `schedules/` 無 xlsx | manifest `files: []`；頁面同現況空狀態 |
| manifest 不存在且未執行 predev/prebuild | `fetch` 404；提示手動匯入（開發時應跑 `npm run schedules:manifest`） |
| xlsx 存在但解析失敗 | `parsed = null`；顯示與手動匯入失敗相同之錯誤處理（`showError`）；**不**保留半套資料 |
| GitHub Pages 子路徑 | 使用 `BASE_URL` 後仍可 `fetch` 到 `…/DrivingInstructorShiftSchedule/schedules/manifest.json` |

### 11.7 版控與部署（已定案）

| 項目 | 決策 |
| --- | --- |
| **xlsx 納入 git** | **是（必須）**。靜態站點沒有後端；預設匯入只能 `fetch` **已隨 repo／`docs/` 部署**的檔案。不 commit xlsx 則線上／clone 後**沒有檔可載**，僅剩手動匯入。 |
| **manifest 納入 git** | **是**。與 xlsx 一併 commit；`prebuild`／`predev` 仍會重產，避免忘記更新。 |
| **`.gitignore`** | **不要**忽略 `public/schedules/*.xlsx`（本專案路線）。 |

### 11.8 建議實作順序

1. 新增 `public/schedules/.gitkeep`（可選）與 manifest 產生腳本 + `package.json` 的 `schedules:manifest`、`prebuild`、`predev`。
2. 以 1～2 份範例 `shift-*.xlsx`（可沿用 `example/` 內測試檔複製並重新命名）驗證 manifest 內容與 `latest`。
3. `src/main.js`：抽出共用載入函式 + 啟動時 `fetch` 預設檔。
4. `npm run build` → 確認 `docs/schedules/manifest.json` 與 xlsx 皆存在；`vite preview` 驗證子路徑 `base`。
5. 手動匯入覆寫、解析錯誤、空 manifest 回歸。

### 11.9 測試計劃（建議）

| 案例 | 預期 |
| --- | --- |
| `schedules/` 有 `shift-20250515`、`shift-20250518` | 開站載入 `20250518`；人員 dialog 與月曆有資料 |
| 僅舊檔 | 載入該舊檔 |
| 檔名 `shift-20251399.xlsx` | `schedules:manifest` **失敗**；終端機標明無效日期 |
| 檔名 `other.xlsx` | **失敗**；終端機標明預期格式；不產生 manifest |
| 目錄內僅合法 xlsx | build 成功；開站載入 `latest`；header 顯示檔名 +「自動」 |
| 手動匯入後 | header 檔名改為本機檔名；badge 消失 |
| `npm run dev` 前未產 manifest | 404 提示；手動匯入仍可用 |
| 預設載入後手動匯入另一檔 | 月曆改為手動檔內容 |
| GitHub Pages `base` | `fetch` 路徑正確、無 404 |
| baseline | 開站後 **未** 自動設定 `baselineParsed`；比較 dialog 仍為「尚未匯入上一版」 |

### 11.10 產品定案（2026-05-18）

| # | 議題 | 決策 |
| --- | --- | --- |
| 1 | xlsx 是否 commit | **必須** commit 至公開 repo（見 §11.7） |
| 2 | 成功載入是否顯示檔名 | **要**；UI 見 §11.5.4（`#schedule-source` + 自動 badge） |
| 3 | manifest 是否 commit | **要** |
| 4 | 檔名不符合規則 | **fail build**；終端機**逐檔**列出原因（§11.4.4）；僅在無法逐檔說明時才降級 warning |

### 11.11 修訂紀錄

| 日期 | 說明 |
| --- | --- |
| 2026-05-18 | 初版：預設載入最新 `shift-YYYYMMDD.xlsx`；`public/schedules/` + 建置前 manifest；啟動 `fetch`；baseline 不預設匯入 |
| 2026-05-18 | 產品定案：xlsx／manifest 皆 commit；header「目前排班表」UI；建置檔名驗證 fail + 逐檔錯誤訊息 |


