{
  "name": "shift-schedule-web",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "schedules:manifest": "node scripts/generate-schedules-manifest.mjs",
    "predev": "npm run schedules:manifest",
    "dev": "vite",
    "prebuild": "npm run schedules:manifest",
    "build": "vite build",
    "preview": "vite preview",
    "parse-test": "node scripts/parse-test.mjs",
    "compare-test": "node scripts/compare-test.mjs"
  },
  "dependencies": {
    "xlsx": "^0.18.5"
  },
  "devDependencies": {
    "vite": "^5.4.0"
  }
}
